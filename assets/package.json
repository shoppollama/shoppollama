{
  "name": "shoppollama_assets",
  "version": "1.0.0",
  "description": "Frontend assets for Shoppollama",
  "scripts": {
    "deploy": "esbuild --bundle js/app.js --outdir=../priv/static/assets --minify",
    "watch": "esbuild --bundle js/app.js --outdir=../priv/static/assets --watch"
  },
  "dependencies": {
    "phoenix": "file:../deps/phoenix",
    "phoenix_html": "file:../deps/phoenix_html",
    "phoenix_live_view": "file:../deps/phoenix_live_view"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
